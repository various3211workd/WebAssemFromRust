#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../create-wasm-app/.bin/create-wasm-app.js" "$@"
  ret=$?
else 
  node  "$basedir/../create-wasm-app/.bin/create-wasm-app.js" "$@"
  ret=$?
fi
exit $ret
